# FTN-Atelier 项目说明（业务需求 + 开发进度 + 交接文档）

> 本文件为工程交接/进展追踪文档。保留了原始需求蓝图、架构约束，并按实际代码核实标注了每个功能点的完成状态。
> 原始需求蓝图与架构约束已内嵌于本文件各章节。
> 最近更新：**（本轮）SHA-256 校验修复 + 安装包自动清理 + 出包**——①**SHA-256 不匹配根因**：GitHub release asset 的 `digest` 字段格式是 `sha256:<64位hex>`（带前缀），Verifier 拿整串与纯 hex 比较必然失败；实测下载文件 SHA-256 与 digest 的 hex 部分完全一致（下载本身没问题）。修复：`selfcheck` 解析时剥离 `sha256:` 前缀，`verifier` 比较前规范化（兼容前缀/纯 hex、忽略大小写）；新增 `sha256:` 前缀用例。②**安装包自动清理**：`core/update/resume.py` 新增 `cleanup_updates_dir`——下载前自动清理 Data/updates：保留当前目标版本的 .zip/.part/.part.meta.json（断点续传与失败重试不受影响），清理其它历史版本包；apply-*.ps1 保留最近 2 个；pending.json 保留 1 个；backup-* 保留最近 3 个（回滚程序文件备份）；updater-electron.log 超 2MB 重置。③**检测日志**：check_update 并发候选逐源记录（API candidate start/success/fail + update source selected）到 Logs。④**mirrors.py**：新增为唯一 GitHub 网络入口（薄转发至 SourceManager，逻辑单一不重复）。⑤**出包**：便携包显示 v1.0.1、内核最新（17:52）；`FTN-Atelier-Portable-1.0.2.zip` 重新生成（17:51）；源码版本 1.0.2。回归 18 项全过。此前一轮：Update Engine 出包。
> **（上轮）v1.0.0 正式版 + 功能增强**——打包版关闭演示数据（`FTN_PACKAGED=1`，未装基底如实显示「未装+下载此版本」）；版本号 1.0.0；**新功能**：引擎启动失败诊断（日志关键词→修复建议）、模型快速扫描（增量跳过未变化文件）、首页占用趋势小曲线、版本切换预览（venv 策略+配置差异确认）、插件一键更新全部 + 重复检测、数据导出/导入（配置/引擎注册/模型索引）、首启简单引导；**文案自适应**：版本下载提示（无主引擎→下载即设为主引擎 / 有主引擎→归为非主引擎）、插件市场主引擎为空显示「reForge / Forge（自动检测）」；修复网络下载搜索框文字过浅（全局占位符可读）。此前已完成：主引擎化 + 自由多开 + 自检流程修复等。
> **（上轮）UI/功能排错收敛**——修版本页一直「加载中」（后端 `protected_paths` 缺失 + 前端 `Promise.allSettled`）、修关于页打不开（`App.jsx` 缺 `About` import + `handleNavigate` 白名单少 `about` + About 调错 `system()`）、修反复自检（启动门控与健康轮询分离）、更新源藏回后台、偏好页去重复外观、**下载镜像真正接线**（HF 端点接入 `downloads.py`、git 镜像接入版本/插件克隆）、镜像开关与网址补齐（`use_hf_mirror` / `git_mirror`）。
> **（上上轮）环境检测链完整化**（`core/env_detect.py` `/api/system/env` + 疑难解答「环境检测」分区；启动自检并入 CUDA 项 → 11 项；模型管理补记 ✅）+ **外观动画**（粒子/光效/呼吸，非活动暂停，`preference.ambient_effect` + `pages/AmbientLayer.jsx`）＋ 输出目录「不分日期」＋ venv 共享策略 ＋ 定论 **embedding/VAE 均不做预览图**。
> **（更早）基底收敛为 reForge / Forge**：从基底体系撤除 A1111 与 ComfyUI；新增 Forge 基底；版本候选改为**从 GitHub 实时拉取 tags（新→旧，取最新 6 个置顶）＋本机已装版本垫底**；下载/更新/回退自动解析真实 tag（兼容带/不带 v 前缀）。**（本轮修正）reForge 与 Forge 仓库分离**：reForge = `Panchovix/stable-diffusion-webui-reForge`，Forge = `lllyasviel/stable-diffusion-webui-forge`，下载/版本检查/更新/回退/接管全部经 `_repo_of(base)` 取对应仓库。真实版本下载/更新/回退（浅克隆 git）+ ComfyUI 版本号排序修复 + LoRA 详情页。
> **（更早）外观动画完成**：新增「外观动画（AmbientLayer）」——粒子 / 光效 / 呼吸三种背景动画，随主色调同色，窗口切为非活动时自动暂停省资源；AppConfig 新增 `preference.ambient_effect`（none|particles|light|breath），主题页「外观动画」分区 + 设置·用户偏好下拉可选，启动时自动恢复。

---

## 一、项目主旨与定位

- **主旨**：打造媲美甚至超越绘世（秋葉aaaki 开发的 A1111 启动器）的 **SD-reForge 启动器 / AI 创作工作台**。
- **定位**：不只是启动器，而是 **AI 创作工作台 + 模型资产管理器 + 工作流管理工具**。
- **环境**：已有最新 github 开源版 reForge，稳定使用中。
- **语言**：界面与注释尽量简体中文。
- **技术栈**：Electron + React + Tailwind CSS + Framer Motion（前端）｜ Python FastAPI（后端常驻）｜ SQLite（数据库）｜ REST API + WebSocket。仅面向 Windows。

---

## 二、核心功能需求与当前状态

**状态图例**
- ✅ **已实现并验证**
- 🟡 **部分实现 / 骨架可用 / 待完善**
- ⬜ **未实现（列入后续里程碑）**

### A. 一键启动 webUI
| 需求 | 状态 | 说明 |
|---|---|---|
| 启动 reForge（等价于 webui.bat，非独立 cmd 窗口） | ✅ | `/api/engine/start`，后台起进程，输出进统一日志 |
| 自动打开浏览器 | ✅ | 引擎启动后由启动器完成（开关可配 `start_args.open_browser`） |
| 检测启动状态 | ✅ | `/api/engine/status`，首页灯带「运行中」实时显示 |
| 显示启动日志 | ✅ | 独立「控制台」页实时展示，可按来源分区 |
| 失败时显示错误 | 🟡 | 互斥/启动失败提示已有，进程级根因定位待加强 |
| 启动前检测 Python / CUDA / 显存 / Git / 模型路径 | ✅ | 启动自检引导已实现（`core/selfcheck.py` 11 项：后端/SQLite/目录/主引擎路径/主引擎 Python/Git/**CUDA·显卡**/网络/配置/端口/孤儿进程）；疑难解答页另有「环境检测」分区（`core/env_detect.py` `/api/system/env`）一键展示 Python/Git/显卡/模型路径（CUDA 驱动文本解析项已移除，保留结构化显卡检测） |

### B. 多启动模式（自由多开）
| 需求 | 状态 | 说明 |
|---|---|---|
| 主引擎（默认） | ✅ | 单一「主引擎」条目（webui 引擎），可改名、不可删；**可「清空路径」解除配置（条目保留）**；未配置时首页占位「暂无主引擎」。**引擎家族**：条目显示 reForge/Forge/A1111/ComfyUI/未知/脚本 徽标；forge/reforge 家族可无缝切换，其它家族弹风险确认后进入「仅启动/停止/重启」受限模式 |
| WD1.4 / LoRA / FTN-tag 等 | ✅ | 不再预置，需要时「设置→新增引擎」添加（batdir / ftn_tag 等类型） |
| 可编辑引擎（增/删/改名/重排） | ✅ | 引擎注册表 `/api/engines`，支持增删改名、路径设置/清空、启动文件检测/手动指定 |
| **自由多开（无互斥）** | ✅ | 取消多开开关与全局锁：任意引擎可叠加启动（主引擎 + N 个），每实例独立进程/日志/端口/关闭；端口冲突自动改空闲端口，首页显示占用（内存/显存）供用户判断 |

### C. 模型管理
| 需求 | 状态 | 说明 |
|---|---|---|
| AssetManager 抽象层 + SQLite 索引 | ✅ | `core/asset_manager.py`，支持扫描/列表/统计 |
| 模型分类（SD/SDXL、Embedding、原生/插件 LoRA、VAE） | ✅ | `/api/models` 支持按 type 查询；模型页分类 Tab（全部/底模/原生LoRA/插件LoRA/Embedding/VAE）+ 每类计数 |
| 打开所在文件夹 / 刷新 / 添加（剪切至模型目录） | ✅ | 模型卡「📂 文件夹」(`/api/models/{id}/dir`)、全量扫描、剪切式「＋添加模型」(`/api/models/add` 多选/同名冲突加序号/自动索引) |
| 模型卡片（大小/基底/预览图/日期） | ✅ | 底模/LoRA 竖版预览图 + 自动读同名 png；**embedding/VAE 用宽条图标卡片（无大图）**。预览图仅 checkpoint/lora/lora_plugin 三型需要：**VAE 由文件名即可识别、embedding 是纯向量权重无图可看，均定论不做预览图**（设计非遗留任务）。大小、基底 badge、触发词、文件更新日期、📂打开文件夹 |
| LORA 详情（预览/触发词/源训练 tag/基底/推荐权重） | ✅ | LoraMetadataProvider 已接入（`core/lora_provider.py` SafetensorsMetadataProvider → 统一 LoraMetadata）；`/api/models/{id}/lora`；前端模型卡片「LoRA 详情」弹窗 |
| 触发词/训练 tag 切换开关、重读覆盖、手动修改 | ✅ | 弹窗内：训练 tag「显示全部源标签」开关、基底「重读」回原始值 + 手动编辑、触发词/推荐权重/备注编辑、「保存覆盖」（`/api/models/{id}/lora/override` 持久化到 app_meta） |

### D. 外观系统
| 需求 | 状态 | 说明 |
|---|---|---|
| 主题系统（亮/暗 × 多主色，4:6 普通:偏好） | ✅ | 完整调色板、切主色全界面跟随、独立「主题」页、全局过渡 |
| 自定义首页头图 | ✅ | 主题页选图/移除/预览，`/api/hero` 头像读取，未设用主色渐变 |
| 简单动画（粒子/光效/呼吸，非活动暂停） | ✅ | `pages/AmbientLayer.jsx`（Canvas 粒子/光效＋CSS 呼吸）随主色调同色；`AppContext` 跟踪窗口活动（visibilitychange/blur/focus），非活动自动暂停；`preference.ambient_effect` 持久化 |

### E. 网络下载
| 需求 | 状态 | 说明 |
|---|---|---|
| CivitAI 搜索/下载模型 | ✅ | 独立「网络下载」菜单页：CivitAI 搜索（类型过滤）→ 直接下载；流式后台任务 + 进度；落入主引擎对应分类目录并自动重新入库（`core/downloads.py`） |
| 连接 HuggingFace 搜索/下载 | ✅ | 独立「网络下载」菜单页：HF 模型库搜索 → 文件选择（按后缀优先级排序）→ 下载；需 Token 的受限仓库若未配置则优雅降级提示 |
| 更新 reForge / 插件（不改设置只更新变化） | ✅ | 真实 git 下载/更新/回退：`/api/versions/download`（浅克隆 --depth 1）+ `/api/versions/{id}/update`（git fetch → checkout 最新候选）+ `/api/versions/{id}/rollback`（git fetch --unshallow → checkout 旧 tag）；后台任务 + 进度轮询，前端版本页 active 行「更新到最新 / 回退」 |

### F. 插件管理
| 需求 | 状态 | 说明 |
|---|---|---|
| 显示插件（LoRA/ADetailer 等）+ 版本 + 状态 | ✅ | 「版本」页子分区「插件管理」：浏览/开关当前主引擎已装插件、git 标记、打开插件目录 |
| 启用/禁用插件 | ✅ | `.ftn_disabled` 标记 + `/api/plugins` 开关 API（插件管理内实时开关，无需进 webui） |
| 浏览 / 更新 / 卸载 / 下载新插件（搜/URL 装） | ✅ | 子分区「插件市场」：仅展示主引擎**通用/专属**插件，支持搜索 + 分类筛选 + 下载 / 更新 / 卸载 / URL 安装（`/api/plugins/market`） |

---

## 三、菜单 / 页面结构与状态

菜单位于左侧（图标 + 四字内文字）。

| 导航 | 状态 | 说明 |
|---|---|---|
| 首页 | ✅ | 顶栏＋高头图(300px)＋引擎启动(灯带单选/启动-停止-重启/多开一个)＋**各引擎占用显示（内存/显存，进程树汇总；html 无）**＋快捷文件夹(主引擎根目录/文生图单图·网格/图生图单图·网格)。**固定一屏不滚动**；未配置主引擎时占位「暂无主引擎」 |
| 模型 | ✅ | 全部类型筛选 + 搜索 + 全量扫描 + 添加（剪切）+ 打开文件夹 + 预览图 + LoRA 详情；空环境即为空，计数符合真实索引 |
| 版本 | ✅ | 子分区：基底版本 / 插件管理 / 插件市场。基底版本跟随「设置」主引擎（不自行切换）；**未安装的基底一律显示「未装」，不再标「主」**；当前版本置顶突出「当前」；更新保护清单；插件市场按当前主基底提供通用/专属插件 |
| 主题 | ✅ | 主色卡片预览＋首页头图自定义（预览高度 300px 与首页一致） |
| 控制台 | ✅ | **独立页·自动挂载**。浏览器式 Tab＝客户端日志（固定）＋**引擎启动带出的 cmd**（自动挂载：每开一个自动新增、名字与引擎对应 主引擎 / 主引擎 #2 …；实际运行才出现，不占位不锁名），每个实例独立日志输出/关闭（×，仅终止对应实例）；引擎进程已隐藏（不弹独立 cmd 窗）；实时 WS＋打开日志目录＋日志文件自动保留最近30份（**无新建会话、无重载按钮、无正常/警告/错误图例**） |
| 疑难解答 | ✅ | 统计卡片（正常/警告/错误计数）＋**单一统一日志列表**（可按级别筛选查看、点击开日志文件）＋**勾选逐条导出诊断包** |
| 小工具 | ✅ | 网站分组：模型社区(Civitai/HF)、标签社区(Danbooru/Gelbooru/Rule34/Safebooru/e621)、学习；http 走 shell.openExternal |
| 网络下载 | ✅ | 独立菜单页（见上方导航表）|
| 设置 | ✅ | 引擎路径(可编辑/更改路径/重新检测入口)×启动参数(显卡自动检测多选/显存模式中文)×环境配置×用户偏好(语言跟随系统/主题/日志级别)×网站API(CivitAI/HF凭据)×**软件修复更新**(启动自检/自动检测更新开关+立即自检)；快捷文件夹：默认 5 个「更改路径 + 重新检测」，额外文件夹仅「更改路径」 |
| 网络下载 | ✅ | **独立菜单页**。来源标签（CivitAI / HuggingFace，含凭据已配/未配徽标）+ 搜索栏（CivitAI 支持类型过滤）+ 结果卡片（CivitAI：图片/类型/基底/NSFW/下载；HF：下载量/点赞/标签/文件选择弹窗）+ 下载任务进度条（轮询）+「下载完成→加入模型库」一步到位 |
| 关于 | ✅ | **独立「关于」页**：品牌卡 + 运行环境（OS/架构/Python/后端版本）+ 数据状态（模型数/配置数/数据库）+ **使用声明（合规条例：禁盗版收费/黄赌毒等）**；页脚「生图引擎工作台」 |
| 模型卡片 | ✅ | 底模/LoRA 预览图、类型徽标、大小、基础模型 badge、LoRA 触发词（前三）、**文件更新日期**（mtime）；「下载模型」按钮改为跳转独立「网络下载」菜单页 |

### 各页面关键实现细节
- **首页引擎启动**：灯带单选替代下拉；启动/停止合并按钮、运行后自动变「停止」；**无路径→启动按钮暗淡锁定**；运行中可「多开一个」（自由多开）；未配置主引擎显示「暂无主引擎」。
- **设置·引擎路径**：主引擎（默认唯一）不可删，**有根目录时可「清空路径」解除配置（条目保留，入口覆盖一并清除）**；增删改名（内联编辑）；已删多开开关（自由多开）；**根目录未设置显示「未设置引擎根目录」、启动文件未设置显示「未设置启动文件」框**；**详细路径可手动改 + 保存**；**重新检测入口**。
- **设置·启动参数·显卡**：`/api/system/gpu` 用 nvidia-smi **自动检测显卡名+显存**；**多张多选 / 单张默认选中**；手动索引兜底 + **重新检测**；存 `gpu_ids`(多卡) / `gpu_index`(主卡)，runner 映射 `--device-id` / `--devices-id`。
- **版本**：`active` 版本自动排最前；`switchVersion` 切换；历史版本折叠分组；当前/历史标记；基底版本跟随「设置」主引擎；未装基底（Forge）显示「未装」+ 下载入口（候选从 GitHub 实时拉取）。
- **版本·插件分区**：顶部三个子 Tab —— 「基底版本」/「插件管理」(浏览+实时开关当前主引擎已装插件+开目录) /「插件市场」(仅主引擎通用/专属插件，搜索+分类+下载/更新/卸载/URL 安装)。
- **主题·头图预览**：预览高度统一为 `h-[300px]`，与首页头图(300px)一致。
- **控制台**：删除了顶栏「重载」按钮与底部「正常/警告/错误」图例标签（日志行内配色仍保留）。
- **疑难解答**：三栏改**单一日志列表**；顶部统计卡片（正常/警告/错误）分列数量；「全部/正常/警告/错误」查看筛选；每条勾选 + 全选当前 + 导出已选条数。
- **设置·快捷文件夹（QuickFolders）**：默认 5 个（root/txt1/txtg/img1/imgg）提供「更改路径」（浏览指定）+「重新检测」（回退自动）；其他额外文件夹仅「更改路径」，不提供「重新检测」（无用）。
- **模型页**：去掉加载时自动注入演示模型逻辑，空环境保持为空、计数与真实索引一致；演示数据仅由人工「全量扫描」时的 demo 触发。
- **控制台/疑难解答**：INFO/WARN/ERROR 用「正常/警告/错误」中文标签并按级别着色；点击可打开日志目录。

---

## 四、架构约束（必须严格遵守）

### 1. 非侵入式管理
- 禁止修改 reForge 核心（modules、核心 py、WebUI 源码）。
- 一切通过：配置注入 / 启动参数 / 外部管理 / 数据索引。

### 2. 用户数据与程序文件分离（V1 稳妥方案）
- **不移动现有 reForge 文件**，不强制迁移 models/outputs/extensions。
- AssetManager 负责扫描、索引现有 reForge 目录，建立统一索引。
- 目标结构（未来方向）：Core/Engines（被管引擎）｜Data（索引/配置/库）｜Backup｜Logs。
- 未来用启动参数/路径映射让多引擎共享资产，第一版不强迁。

### 3. 可编辑引擎（AppConfig 条目式）
- 主引擎（默认 reForge）：可改名、**禁止删除**；**主引擎可切换**（`engine_custom.primary_key` 记录，可指向任意已配置根目录的引擎，`/api/engines/{key}/primary` 按根目录推断基底并同步主基底）。
- 其他引擎（WD/lora/tag…）：可增/删/改名/重排。
- 引擎结构条目化而非固定字段。已通过 `engine_registry` 实现。
- **引擎家族 / 受限模式（本轮）**：`base_registry.family_of(root)` 识别 reforge / forge / comfyui / a1111 / unknown（脚本/工具类 = other）；`set_primary` 对 forge/reforge 家族直接切换并同步主基底，其它家族允许切换但返回 `limited`——只改默认行为，绝不移动/修改/删除模型与用户数据；模型管理（`asset_manager`）、插件（`/api/plugins*`）、模型下载（`/api/downloads/start`）在主引擎受限时返回「不适用」。

### 4. 多启动模式：自由多开（无全局互斥）
- **产品定论（本轮）**：取消多开开关与全局进程锁，任意引擎可叠加启动（主引擎 + N 个其他引擎，或同引擎多开）。
- 每个实例独立进程 / 日志源 / 端口 / 关闭控制；端口冲突由程序自动改空闲端口（webui 改 `--port`，bat 经 `COMMANDLINE_ARGS` 注入），用户按首页占用/端口显示自行判断。
- 前端按钮不再做互斥置灰；控制台逐个关闭实例。

### 5. LoraMetadataProvider 抽象
- 前端**禁止直接读来源数据**，只依赖统一 `LoraMetadata`。
- Provider（Safetensors**已接入** `core/lora_provider.py`；其余待建）：
  - SafetensorsMetadataProvider（`ss_tag_frequency`/`ss_dataset_dirs`/`ss_training_comment`…）✅ `core/lora_provider.py`
  - KohyaProvider（训练目录/caption/metadata）⬜
  - CivitaiProvider（未来）🟡 **已实现** `core/lora_provider.CivitaiMetadataProvider`（在线尽力而为），并在 `asset_manager.lora_detail` 配置 CivitAI Key 后自动补拉真实触发词/推荐权重/基底/简介；断网/无凭据静默跳过
  - ManualProvider（用户手动）✅ 手动覆盖经 `lora_override` 存 app_meta

### 6. AssetManager + SQLite
- 全量扫描→建库；后续增量扫描（路径/大小/mtime，可选 hash）。
- 首次启动全量，后续增量；UI 提供快速刷新 + 完整重建索引。
- ModelAsset 字段支持多引擎（source_type 区分 reForge/ComfyUI/其他），新引擎只加扫描源不改库结构。

### 7. Electron ↔ Python 通信
- Electron 主进程 + FastAPI 常驻 + WebSocket。生命周期：启动→单实例锁→拉起/连接FastAPI→建 WS；退出→通知关闭→释放 pid/lock→清理孤儿进程＋多开保护。
- **该链路已在 M0 验证**（backendManager.cjs：ensureBackendUp / shutdownBackend / forceCleanupOrphan / 端口冲突/孤儿清理）。
- **（本轮）WS 日志推送桥修复**：log_manager 订阅回调为 async 函数但被同步调用（协程从未 await → 实时日志不落地），现于 lifespan 捕获主事件循环 + `call_soon_threadsafe` 调度；`/api/shutdown` 前置清理全部控制台会话防孤儿。

### 8. 日志系统
- 统一 LogManager，管理 reForge/Python后台/更新/错误日志。Windows 统一 UTF-8，支持 GBK/ANSI/UTF-8 编码容错。
- **已增强**：按 source/category 分文件、按天归档、**自动保留最近 30 份删旧**、内存环形缓冲、WS 订阅推送、分级查询（正常/警告/错误跟随语言）。

### 9. reForge 版本管理
- **版本身份最终方案（本轮）**：`install_source` 三分类——
  - `atelier_managed`：存在 `.ftn/engine.json`（记录 engine/repository/branch/commit/install_source/installed_at），由 Atelier 安装/接管，版本身份 `branch @ commit`；
  - `git`：存在 `.git`，读 tag / commit / branch，用 Git 检测更新；
  - `external`：用户自带 ZIP/目录（无 .git、无 Atelier 记录），**版本 Unknown 是合理结果**，绝不猜 commit、绝不把目录名当版本、绝不 git init 伪造。
- **ZIP 接管与更新**：接管（外部→Atelier 管理）与 Atelier Managed 更新共用同一安装例程：GitHub API 取目标 branch 最新 commit → 下载 ZIP → 临时目录解压 → 校验（必须有 launch.py/webui.py）→ 备份用户数据（models/outputs/extensions/embeddings/localizations/random_res_config/venv + 配置类，`PROTECTED_PATHS` + 根目录 *.json）→ 替换程序文件 → 还原用户数据 → 写 `.ftn/engine.json`；**任何失败自动回滚旧程序目录**，成功才删旧备份；更新流程「检测→下载→解压→校验→停止引擎→备份→替换→还原→启动验证→成功删备份 / 失败回滚」，用户数据（模型/输出/扩展/配置）绝不被无条件删除。
- **UI**：external 显示「来源：外部 ZIP · 版本：Unknown」+「接管为 Atelier 管理」；atelier_managed 显示「来源：Atelier 管理 · 版本：main @ 8f31abc · 状态：✓ 最新」+「检查更新」；git 显示 tag/commit +「选择新版 / 回退」。无正式版本号时**不伪造 semver**。
- **tag 过滤（本轮修正）**：`_fetch_tags` 接受「数字.数字… + 可选尾缀字母」的版本 tag（`v1.7.0d`、`1.10.1RC`），纯字母/浮动 tag（`latest`）不算版本、排除；接管/外部实例无 tag 时，仓库全部正式 tag 进入回退候选。**reForge 官方当前正式 tag 只有 `v1.7.0d`（`latest` 为浮动 tag）**，因此「只有一个可回退历史版本」是仓库真实情况，不是漏检；`main @ 739b2e1` 为当前 main 分支 HEAD，非版本号。
- **候选语义（本轮）**：下载候选 = `main（最新）` + 正式 tag（v1.7.0d）+ `previous`（仅 Forge，官方浮动标记）；**删除假静态候选**（1.11.0/1.10.2 等在官方仓库无对应 tag、下载必失败）。Clone 版更新 = `git fetch origin` + `checkout --detach origin/<branch>`（滚动更新，commit 即版本）；回退 = 正式 tag / previous。外部 ZIP 新增「绑定版本」（`.ftn/bind.json`，用户填 commit/tag，仅作展示，不伪造 .git）。
- **备份上限（本轮）**：旧程序备份 `Backup/engine-backups` 保留最近 2 份；用户数据救援备份 `.ftn-userdata-backup-*`（还原失败才生成）保留最近 3 份，更旧自动清理；插件卸载备份 `.uninstalled` 单份覆盖；日志每类保留 30 份。所有备份均有上限，不会无限堆积。

### 10. 更新保护清单
models/  outputs/  extensions/  embeddings/  localizations/  ui-config.json  config.json  webui-user.bat  styles.csv  random_res_config/  *.json  + venv/（大版本需重建：检测依赖→询问→保留/重建）。
> 已列入版本页「更新保护」展示。

### 11. 输出目录管理（不分日期）
- 目标取消默认日期子目录，直接扔 outputs/ 根。

- 方案：reForge 无「关日期」启动参数 → 采用「监听 + 迁移」（不碰源码）：后台轮询 `outputs/<mode>-images/<日期>/`，把新增图片 move 到 `outputs/<mode>-images/` 根并删空日期目录。→ **✅ 已实现**：`core/output_watcher.py`（单例后台线程，3s 轮询，仅整理结尾 `-images` 目录里的纯 8 位日期子目录、仅移动图片、同名冲突自动加序号）+ `output_watcher.start()` 启动时按配置起线程 + API `GET/POST /api/outputs/auto-organize`（开关）、`POST /api/outputs/organize-now`（立即整理）+ 设置「输出整理」Tab 开关/立即整理。

---

## 五、开发里程碑进度

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M0 脚手架 | Electron+Vite+React+Tailwind+Framer+FastAPI常驻+通信+WS+生命周期+多开 | ✅ 完成并验证 |
| M1 基础模块 | AppConfig/LoraMetadata/ModelAsset 数据结构、SQLite 初始化、单实例锁、LogManager | ✅ 完成 |
| M2 reForge 控制 | 首页UI、一键启动、进程状态、实时日志 | ✅ 完成（日志已移至独立控制台页） |
| M3 配置与路径 | 设置页、路径管理、启动参数管理 | ✅ 完成 |
| M4 AssetManager | 全量/增量扫描、模型索引、缩略图、模型管理UI | ✅ 索引扫描可用；模型卡片（底模/LoRA 预览图、embedding/VAE 图标卡片、大小/基底/日期/📂开文件夹、触发词）；LoRA 详情弹窗。**VAE/Embedding 预览已定论不做** |
| M5 版本管理 | reForge 版本隔离、更新保护、配置备份、回滚 | ✅ 版本隔离/保护/切换完成；**真实 git 下载/更新/回滚 完成**（浅克隆 --depth 1 + 回退 --unshallow；后台任务 + 进度轮询） |
| M6 扩展 | 网络下载、插件管理、主题系统 | ✅ 插件(浏览/开关/市场)、主题、网络下载 均完成 |

---

## 六、本次会话完成明细（按本轮要求逐项）

1. **首页**：FTN Studio→**FTN Atelier**；头图加高到 300px（当前值）；**移除控制台**（转独立页）；去掉 tag 库大按钮；去引擎副标题；**固定一屏不滚动**；无路径→启动按钮暗淡锁定。
2. **控制台独立页**：多引擎/客户端日志分区（自动排除 html/tag 库进程）；实时 WS；INFO/WARN/ERROR 跟随语言（正常/警告/错误）；点标签开日志目录；日志自动保留最近 30 份删旧。**（最新：已删顶栏「重载」按钮与底部「正常/警告/错误」图例标签）**
3. **疑难解答**：（最新）统计卡片正常/警告/错误计数＋**单一统一日志列表**（可按级别筛选、点击开日志文件）＋**勾选逐条导出诊断包**。
4. **显卡自动检测**：nvidia-smi 显示显卡名+显存；多张多选/单张默认；手动兜底+重新检测；`gpu_ids` 多卡映射到 runner。
5. **设置**：引擎「详细路径」手动改+保存+**重新检测入口**；GPU 多选 UI；显存模式中文注释。
6. **版本页**：绿色硬编码全改主题色；当前基底置顶+突出；历史版本分组；适配多基底说明；**插件管理区块**（浏览/开关/开目录）。
7. **小工具**：分组扩充（模型社区/标签社区/学习），http 走 `shell.openExternal`。

### 最近一轮修正明细（UI/功能打磨）
1. **模型页**：去除加载时自动注入演示模型逻辑（之前 2/1 计数是演示占位导致）；空环境即真实空，演示数据仅由人工全量扫描的 demo 触发。
2. **版本页·基底**：演示数据只对 reForge（主基底）；A1111 未装显示「未装」而不是可选切换；删掉「『X』为其他引擎」说明文本；删掉「自己选主基底」下拉，基底跟随「设置」主引擎。
3. **版本页·插件分区**：拆成顶部三 Tab —— 基底版本 / **插件管理**（浏览+实时开关+开目录）/ **插件市场**（仅主引擎通用/专属插件，搜索+分类+下载/更新/卸载/URL 安装；去掉了基底筛选下拉与「打开插件目录」按钮）。
4. **主题页头图预览**：`h-40`(160px) → `h-[300px]`，与首页一致。
5. **控制台**：删顶栏「重载」按钮、删底部「正常/警告/错误」图例标签。
6. **疑难解答**：三栏合并为单一统一日志列表；顶部统计卡片（正常/警告/错误计数）；「全部/正常/警告/错误」查看筛选；每条日志勾选 + 全选当前 + 导出已选条数。
7. **设置·快捷文件夹**：「指定目录 + 手动路径」改为「更改路径」（浏览指定）＋「重新检测」（回退自动）；仅默认 5 个（root/txt1/txtg/img1/imgg）提供「重新检测」，其他额外文件夹仅「更改路径」。

### 验证记录（本次）
- 前端 `npm run build` ✅；后端 `py_compile` ✅。
- 首页渲染 8976 字符无运行时错误、无白屏（已修 `connected is not defined`）。
- `/api/system/gpu` → 识别到 RTX 3080 Ti 12GB ✅。
- `/api/logs/sources` → 客户端 + reforge + wd + lora 分区 ✅。
- `/api/plugins` → 插件列表（真实实例显示 extensions）✅。

### 验证记录（最近一轮）
- 前端 `vite build` ✅（45 modules 通过，含 Troubleshoot / Versions / Console / Settings / Theme / Models 改动）；后端 `py_compile core/version_manager.py` ✅。
- 版本页：reForge 演示版本保留、A1111 显示未装；plugin 管理/市场两子分区、插件管理加载中/为空/有列表三态均有兜底。
- 疑难解答：单列表 + 统计卡 + 级别筛选 + 勾选导出（空筛选、加载态均兜底）。
- 主题页头图预览高度 300px 已验证；控制台无重载/无图例。

### 本轮新增大项（启动自检 / 软件修复更新）
1. **启动前自检引导**：打开软件先跑 10 项自检（后端 / SQLite / 目录完整 / ReForge路径 / ReForge Python / Git / 网络 / 关键配置 / 端口 / 孤儿进程），完成才进入主界面。
   - 后端 `core/selfcheck.py`（`/api/selfcheck/run`、`/api/selfcheck/fix`、`/api/selfcheck/update`）；前端 `SelfCheckModal.jsx` 全屏引导（startup 阻塞门控）＋设置「立即自检」（manual）。
   - 可修复项（目录/sqlite/config/orphan）询问「是否修复」→ 自动修复；不可修复项（reforge路径/Python/git/网络）提示「无法自动修复，请检查」。
2. **版本更新检测（更新源可配置）**：`/api/selfcheck/update` 探测 GitHub releases/latest。更新源 `owner/repo/mirror` 存于配置（`selfcheck.update_owner / update_repo / update_mirror`）——**开发环境留空则跳过更新检查、不发网络请求（config_missing=true）**，不硬编码/假设仓库地址；遇限流/断网等非配置问题优雅降级「无法检测」。正式发布时在设置→软件修复更新填写真实 owner/repository 即可启用。
3. **设置·软件修复更新 Tab**：新增 `SelfCheckConfig`（`selfcheck.config` 持久化）——「启动自检」「自动检测更新」两个开关注入 AppConfig (`run_on_startup` / `check_update_on_startup`，默认开)＋「立即运行自检」。
4. **本轮还完成**：模型预览图仅保留底模/LoRA（embedding/vae 用图标占位）、模型「📂 打开文件夹」(`/api/models/{id}/dir`)、剪切式「＋添加模型」(`/api/models/add`)、控制台日志源改「类浏览器多标签」。
### 验证记录（本轮）
- 前端 `vite build` ✅（404 modules：新增 SelfCheckModal、DownloadModal）；后端 `py_compile` ✅。
- 自检端到端：10 项全跑通（后端/sqlite/目录/git/网络/config/port/orphan ok；reforge 路径未配置→error 不可修、reforge python→warn）；`fix dirs` 返回 ok ✅；`update` 遇 GitHub 403 优雅返回 error ✅。
- 模型 add_models（多选/剪切/同名冲突 `(1)`/入库+删旧索引）、model_dir（返回正确分类目录）、config api_keys & selfcheck 配置深度合并持久化 ✅。

### 本轮新增大项（真实版本管理 + LoRA 详情）
1. **ComfyUI 版本顺序修复**：`candidate_versions()` 加语义化版本排序 → 一律新→旧（comfyui: v0.3.48→v0.3.33→v0.3.25→v0.3.7→v0.2.2；a1111: 1.10.1→1.8.0）。
2. **真实版本下载/更新/回退**（参考绘世/StabilityMatrix 模型，浅克隆 + unshallow）：
   - 后端 `version_manager.py` 后台任务执行器 + 流式 git 进度解析（`_stream_git`/`_parse_clone_pct`）。
   - `POST /api/versions/download`（`git clone --depth 1 --branch tag` → `Core/Engines/<base>/`）+ `GET /api/versions/download/status/{task_id}` 进度轮询。
   - `POST /api/versions/{id}/update`（浅克隆先 `git fetch --unshallow`，再 `git fetch --tags` → `git checkout` 最新候选）。
   - `POST /api/versions/{id}/rollback`（`--unshallow` → `git checkout <旧tag>`）。
   - 前端版本页 active 行「↑更新到最新 / ↶回退」+ 顶栏后台任务进度条（% + 实时日志）。
3. **LoRA 详情页**（SafetensorsMetadataProvider 接入）：
   - 新建 `core/lora_provider.py`：从 `.safetensors` 头部 `__metadata__` 解析 → 统一 `LoraMetadata`（`ss_tag_frequency`→触发词/训练tag、`ss_sd_model_name`→基底、训练信息、network/optimizer/scheduler）。
   - `asset_manager`：`lora_detail()`（Provider + 手动覆盖合并）、`lora_override()`（覆盖持久化 app_meta `lora_override:{id}`）、`get_by_id()`。
   - API：`GET /api/models/{id}/lora`、`POST /api/models/{id}/lora/override`。
   - 前端：模型卡「📖 LoRA 详情」按钮 + `LoraDetailModal.jsx` 弹窗（训练 tag「显示全部源标签」开关、触发词编辑、基底「重读」回原始值+手动编辑、推荐权重、训练信息、自定义备注、保存覆盖）。

### 验证记录（本轮）
- 前端 `vite build` ✅（46 modules，含新增 LoraDetailModal）；后端 `py_compile` 全部 ✅。
- 版本逻辑冒烟：候选排序、tag 构造（1.10.1→v1.10.1 / v0.3.25→v0.3.25）、clone 进度解析（45%）、未知基底拒绝 ✅；路由注册 `/api/versions/download|status|update|rollback` 全就绪 ✅。
- LoRA 详情端到端（demo 数据）：`lora_detail` 返回 ok + 名称/基底/训练tag ✅；`lora_override` 保存 base=SDXL、权重 0.8 并持久化 ✅；checkpoint 详情正确拒绝 ✅。

### 本轮新增大项（输出目录整理 + venv 共享策略）
1. **输出目录「不分日期」→ 监听+迁移（`core/output_watcher.py`）**：
   - reForge 无「关日期」启动参数，故不碰源码、采用后台整理：单例线程 **3 秒轮询**，扫描主引擎 `outputs/<mode>-images/<日期>/`，把图片 move 到 `outputs/<mode>-images/` **根目录**并删空日期目录。
   - 安全规则：仅整理结尾 **`-images`** 的目录、仅**纯 8 位数字日期**子目录、仅**图片文件**（png/jpg/jpeg/webp/gif）、**同名冲突自动追加 `(1)` 序号不覆盖**。
   - 接线：`output_watcher.start()` 后端启动时按配置起线程；API `GET/POST /api/outputs/auto-organize`（开关）、`POST /api/outputs/organize-now`（立即整理）；AppConfig 新增 `output_auto_organize`；设置新增「**输出整理**」Tab（开关 + 立即整理）。
   - 注：reForge 与 Forge 为不同仓库（reForge=Panchovix/stable-diffusion-webui-reForge、Forge=lllyasviel/stable-diffusion-webui-forge），两者默认都按日期分目录；设置页提示文案已注明两者。
2. **venv 共享策略（`version_manager.venv_strategy`）**：
   - 对齐蓝图「小版本复用、大版本重建询问」：`venv_share`（AppConfig 顶层，默认开）关闭 → 每版本独立 `rebuild`；开启且该基底已有共享 venv → 按 target 相对当前版本的 **major.minor 是否变化**判 `reuse` / `rebuild`。
   - API `GET /api/versions/{base}/venv-strategy?target=`；设置·环境配置 Tab「多版本共享环境」开关。
   - 设置页 `update` 助手新增 `__top__` 顶层字段分支，使 `venv_share` 正确落盘顶层（非嵌套）。
   - 说明：demo 模式 `current()` 恒为 None，故跨版本触发判断在真实安装实例下生效；`_num_tuple` 比较已单独验证（1.11→2.0 判重建、1.11.0→1.11.1 判复用）。
3. **定论：embedding/VAE 均不做预览图**：模型页 `PREVIEW_TYPES = ['checkpoint','lora','lora_plugin']`，embedding/VAE 沿用「宽条图标卡片（无大图）」。VAE 由文件名可识别（`vae-ft-mse-*`/`kl-f8-*`）、embedding 为纯向量权重本就不该有图，**非遗留任务**。

### 验证记录（本轮）
- 后端 `py_compile` ✅（version_manager / output_watcher / app_config / main）；前端 `npm run build` ✅。
- 输出整理沙箱实测：临时目录迁移 2 文件→根目录、删 1 空日期目录 ✅；同名冲突序号不覆盖逻辑就绪。
- 接口端到端：`GET/ POST /api/outputs/auto-organize`（enabled 开关 + 启停线程）、`POST organize-now`、`GET /api/versions/reforge/venv-strategy` 均返回正确 JSON ✅；config 暴露 `venv_share` / `output_auto_organize` ✅。
- ⚠️ 部署提示：曾因同消息并行编辑 `main.py` 导致输出接口块被覆盖丢失，已 grep 排查补回并全量重测通过——后续若「改了接口不生效」，多为并行编辑冲突，重启后端即可。

### 本轮新增大项（外观动画 AmbientLayer）
1. **外观动画层（`pages/AmbientLayer.jsx`）**：在应用内容背后渲染轻量环境动画，可选三种：
   - `particles` 粒子：少量主色光点上浮飘移、闪烁，Canvas 绘制（`useParticles`）。
   - `light` 光效：大号柔光斑流动漂移、明暗呼吸（`globalCompositeOperation=lighter` 叠加，`useLights`）。
   - `breath` 呼吸：纯 CSS 径向渐变 `ftn-breath` 明暗脉动。
   - 三种效果均通过 CSS 变量 `--color-accent` 与当前主题主色同色；粒子/光效共用同一 `<canvas>`，用 `active` 标志保证同一时刻只有一种动画在渲染（避免互相覆盖）。
2. **非活动自动暂停**：`AppContext` 新增 `windowActive`（监听 `visibilitychange` / `blur` / `focus`）。窗口切为非活动（最小化/切走）时：粒子/光效停止推进运动（定格当前帧）、呼吸动画 `animation:none` 并淡出，省资源。
3. **配置持久化**：AppConfig `UserPreference` 新增 `ambient_effect: none|particles|light|breath`（默认 `none`）；`config_manager` 深度合并天然支持，启动时 `App.jsx` 恢复已存效果。
4. **UI 入口**：主题页新增「外观动画」分区（4 选项卡片：无/粒子/光效/呼吸，实时预览＋保存）；设置·用户偏好新增「外观动画」下拉（与主题页同步同一配置）。
### 验证记录（本轮）
- 前端 `npm run build` ✅（405 modules，含新增 AmbientLayer）；后端 `py_compile` 全部 ✅。
- 配置端到端：`GET /api/config` 暴露 `preference.ambient_effect`（默认 none）✅；`PUT /api/config`（preference.ambient_effect=particles）深度合并持久化并回读一致 ✅，已还原默认。
- 修复记录：AmbientLayer 曾存在同一 canvas 上两类动画同时渲染的问题（粒子/光效用 `active` 标志守卫解决）；AppContext 因编辑覆盖曾丢失 `useApp`/`BACKEND_URL` 导出（已整文件重建恢复）。

### 本轮新增大项（环境检测链完整化）
1. **环境检测模块（`core/env_detect.py`）**：`/api/system/env` 聚合只读环境快照——操作系统、Python、Git、CUDA 驱动（Driver/ CUDA 版本）、GPU（nvidia-smi：索引/型号/显存/驱动）、模型路径完整性（主引擎 models/Stable-diffusion、Lora、VAE、embeddings、outputs）。展示于疑难解答页新增「环境检测」分区（卡片状态灯 + 重新检测）。
2. **启动自检并入 CUDA**：`core/selfcheck.py` 新增 `cuda` 检测项（nvidia-smi 探测 GPU/显存），启动前自检由 10 项 → **11 项**，进一步对齐蓝图「启动前检测 Python/CUDA/显存/Git/模型路径」。
3. **apiClient**：新增 `systemEnv()`。
### 验证记录（本轮）
- 后端 `py_compile` 全量 ✅；前端 `npm run build` ✅（405 modules，含 Troubleshoot 环境分区）。
- 环境检测端到端：`GET /api/system/env` 返回 os/python/git/cuda_driver/gpu/model_paths 6 项（本机检测到 Windows、Python 3.10.8、Git 2.40、**Driver 596.49 / CUDA 13.2、RTX 3080 Ti 12288MB**；模型路径未配置 ReForge → warn 合理降级）✅。
- 自检端到端：11 项全跑通，`cuda` 项识别到 RTX 3080 Ti ✅。

### 本轮新增大项（UI/功能排错收敛 —— 版本加载、关于页、反复自检、镜像接线）
1. **版本页一直「加载中……」（真 Bug，已修）**：
   - 根因：后端 `/api/versions/protected` 返回 500 —— `version_manager.protected_paths()` 方法从未定义（只有 `PROTECTED_PATHS` 常量），触发 `AttributeError`；而前端版本页 `load()` 用 `Promise.all()` 并行请求 快照/保护路径/插件，任一失败整个 Promise 拒绝 → `data` 永不赋值 → 页面永久「加载中」。
   - 修复：后端补 `protected_paths()`（返回 `{"items": [...]}`）；前端 `Versions.jsx` 的 `load()` 改用 `Promise.allSettled()`，单端点失败不再拖垮整页，并给出错误提示。
2. **关于页打不开（潜在 Bug，已修）**：`App.jsx` 从未 `import About`，且 `handleNavigate` 白名单缺 `'about'`（Layout 有「关于」入口但点了不跳转）。已补 import + 加白名单；另 About 页调用 `backendApi.system()`（不存在，应为 `systemInfo()`）已改；后端 `/api/system` 补 `version` 字段对齐页面展示。
3. **反复自检（已修）**：`App.jsx` 启动自检门控原来挂在 5s 健康轮询里反复触发。现拆成独立 `startup()`（仅启动执行一次，按 `run_on_startup` 决定进自检或直接主界面），普通轮询 `check()` 只更新 `backendStatus`、不清 `gate` → 不再反复弹出。用户若要彻底跳过可到设置→软件修复更新关「启动自检」。
4. **软件修复更新「更新源」藏回后台**：`Settings.jsx` 移除 Owner/Repository/镜像可编辑输入框，只留只读版本提示 + 「立即自检」+「检测版本更新」。更新源仍由后端 `selfcheck` 配置持有，不向普通用户暴露。
5. **用户偏好去重复外观**：删「用户偏好」Tab 里与「主题」页重复的外观主题卡片 + 外观动画下拉；「输出整理」并入「用户偏好」Tab（删独立 Tab 按钮）。
6. **下载镜像真正接线 + 补开关与网址（环境配置）**：
   - `EnvConfig` 新增 `use_hf_mirror`（bool，默认开）与 `git_mirror`（前缀，默认 `https://ghproxy.com/`）。
   - `core/downloads.py` 新增 `_hf_base()`：HF 搜索/文件列表/下载 URL 均改用配置的镜像端点（之前 `hf_endpoint` 是「摆设」未生效）。
   - `core/version_manager.py` 新增 `_git_url()`：启用 Git 镜像且配置前缀时，把版本实例克隆、插件克隆的仓库 URL 前缀加镜像（tag 探测等轻量 `ls-remote` 仍走原地址，避免 ghproxy 局限）。
   - 设置·环境配置把 PyPI/HuggingFace/Git 三个下载镜像开关 + 各自网址整理成「下载镜像」块，全部即时保存。
### 验证记录（本轮）
- 前端 `npm run build` ✅（387 modules）；后端 `py_compile`/`compileall` 全量 ✅；`node --check electron/main.cjs` ✅。
- 端点端到端：`/api/versions`、`/api/versions/protected`（items=9）、`/api/plugins`、`/api/config`（暴露 `git_mirror`/`use_hf_mirror`）、`/api/system`（version 字段）、`/api/stats`、`/api/downloads/sources`、`/api/system/env` 全部返回正确 JSON ✅。
- ⚠️ 老坑提示：曾存在单条消息内并行编辑 `main.py` 造成接口块被覆盖丢失；凡「改了接口不生效」先 grep `main.py` 该路由是否完整，再重启后端。

### 本轮新增大项（控制台多窗口 / CMD 多开）
1. **会话模型**（`core/console_sessions.py` 新增）：内置控制台改为「会话 Tab」模型——每个会话 = 一个独立子进程 + 独立标题 + 独立日志源（`session-<sid>`）+ 独立关闭控制。
   - `cmd` 会话：`cmd /c <command>`（命令必填，执行完自然退出）。
   - `powershell` 会话：`powershell -Command <command>`（命令必填，执行完自然退出）。
   - **不占位不锁名**：会话 Tab 只由「实际运行的进程」生成；引擎进程由 runner 快照实时合成（`engine:<key>` 伪会话，停止即消失）；CMD/PowerShell 会话创建即出现、结束/关闭即移除（已结束短暂保留 10s 展示「已结束」）。
2. **关闭隔离**：`POST /api/console/sessions/{id}/stop` —— 引擎会话走 runner 生命周期（优雅关闭+互斥释放），超时 `taskkill /PID <pid> /T /F` 整树强杀，**只影响本会话，不影响其他会话/引擎**。**（后续定论：已移除手动新建 CMD/PowerShell 会话，控制台只自动挂载引擎 cmd）**
3. **Windows 进程创建**：`CREATE_NO_WINDOW | CREATE_NEW_PROCESS_GROUP`（输出走管道，内置控制台即窗口，不弹独立 cmd 窗口）；stdout/stderr 合并按 UTF-8→GBK 容错解码写入 log_manager 独立源；退出监控线程补「已结束」标记并回收。
4. **孤儿进程防护**：后端 `/api/shutdown` 前统一 `session_manager.shutdown_all()` 整树清理全部会话；`runner.stop()` 超时强杀升级为 `taskkill /T /F`（防 `cmd /c` 遗留子进程）。
5. **WS 日志推送桥修复（真 Bug）**：`log_manager.subscribe(_push_log)` 原来订阅的是 async 函数，log_manager 同步回调直接调用 → 协程从未被 await，**实时日志实际推不出去**。现于 lifespan 捕获主事件循环，用 `call_soon_threadsafe` 把推送协程调度回 WS 事件循环；并顺带把 `on_event` 弃用告警改为 lifespan。
6. **前端控制台页**（`pages/Console.jsx` 重构）：Tab 条＝客户端（固定）＋各引擎实例会话；会话 Tab 带类型徽标（⌘）、状态角标、关闭 ×；**无「新建会话」面板**（产品定论：引擎启动的 cmd 自动挂载，每开一个自动新增、名字对应）；会话列表 2s 轮询（沿用下载/版本任务轮询模式）。
7. **API**：`GET /api/console/sessions`（自动挂载的引擎实例列表）、`POST /api/console/sessions/{id}/stop`。
### 验证记录（本轮）
- 前端 `npm run build` ✅（406 modules，含 Console 重构）；后端 `py_compile` 全量 ✅；`node --check` 三件 ✅。
- 新增回归测试 `tests/console_sessions_test.py`（4 项全过，后续已随定论改写）：引擎启动→自动挂载（名字对应）、**WS 实时推送引擎日志**（log→WS 桥）、多开→自动新增 #2 且关闭 #2 不影响 #1、停止→会话自动消失。
- 既有测试套件全过：runner / runner_multi / engine_registry / watchdog / data_models / asset_scan / check_tags ✅；`version_test.py` 因 `list_versions()` 早已改为按基底分组返回 dict，测试脚本按列表迭代报错，已同步修正后通过。
- 产品定论（对齐绘世）：移除手动新建会话与交互输入（前端输入条 / 后端 input 接口 / 交互式保活会话 / POST 创建接口全部删除），控制台只自动挂载引擎启动带出的 cmd，每开一个自动新增标签、名字与引擎对应。
- ⚠️ 注意：v1 会话为「内置控制台内的进程输出/查看」形态，不做原生独立终端窗口、不支持发指令；控制台会话为自由命令，不受引擎互斥锁约束。

### 本轮新增大项（引擎真多开 / 关闭拦截 / WS 推送上限）
1. **引擎多开开关真正接线**（设置→引擎路径→「多开」，默认关）：`runner` 从单槽扩展为「主实例 + 额外实例」模型。
   - 多开关（默认）：行为不变——全局互斥，运行中再启动返回「引擎已处于运行中」。
   - 多开开：同引擎或不同引擎可叠加启动；主实例占用互斥语义，额外实例（`#2/#3…`）不占锁、独立进程 / 独立日志文件与日志源（`reforge-2` 等）/ 独立就绪探测 / 独立关闭。
   - 端口偏移：webui 类 .py 引擎的额外实例自动 `port + (num-1)`（`--port=` 或位置参数均替换），避免多实例端口冲突；bat 引擎端口由脚本自身决定。
   - 日志分区修正：主实例日志源由硬编码 `reforge` 改为 `engine_key`（wd/lora 输出终于落到自己的分类文件），额外实例为 `{key}-{num}`。
2. **关闭隔离**：`POST /api/console/sessions/engine:{key}:{num}/stop` 只停指定实例；`POST /api/engine/stop?engine=` 停指定引擎的全部实例；`POST /api/engine/stop-all` 停全部。
3. **关闭应用拦截（Electron）**：`main.cjs` 窗口 close 事件拦截——引擎（含多开实例）仍在运行时弹窗「……运行中，是否关闭？」；「是」→ 先调 `/api/engine/stop-all` 停引擎再退出，「否」→ 取消关闭留在应用；无引擎运行时直接关闭。`/api/shutdown` 也先停全部引擎实例再清理控制台会话（双保险防孤儿）。
4. **WS 日志推送上限**：每 WS 连接待发队列上限 3000 条，超限丢弃新日志（防会话大量刷屏时事件循环被拖垮；前端显示端另有 2 万行截断、REST 端 500 条环形缓冲）。
5. **首页适配**：引擎灯带按「该引擎是否有实例在跑」点亮；选中的多开引擎运行中显示「多开一个」按钮；停止/重启按选中引擎操作（停其全部实例）；html（ftn_tag）引擎仍只有启动键。
6. **控制台引擎会话分类**：仅「有 cmd 的引擎」（webui.bat / 一键启动.bat / launch.py 等脚本启动）进控制台；ftn_tag（html 工具）无 cmd，不占会话 Tab。
### 验证记录（本轮）
- 前端 `npm run build` ✅（406 modules）；后端 `compileall` 全量 ✅；Electron `node --check` ✅。
- 新增 `tests/multi_open_test.py`：默认互斥 → 开启多开双实例并存（pid/日志源独立）→ 关实例 #2 不影响主实例 → stop_all 全停 ✅。
- API 全链路（与前端调用一致）：`/api/engines/reforge/multi?enabled=true` → 两次 start（instance=1,2）→ status.instances=2 → 控制台双 Tab（`engine:reforge` / `engine:reforge:2`）→ 只关 #2 → stop-all ✅。
- 修复：runner 锁由 `threading.Lock` 改为 `RLock`（start 持锁嵌套调用 _start_extra 死锁，已定位并修复）。
- 既有测试套件（runner / runner_multi / engine_registry / version / watchdog / data_models / asset_scan / check_tags / console_sessions）全过，默认互斥行为不变。

### 本轮新增大项（端口冲突自动处理 / 启动自检独立小窗）
1. **端口冲突自动处理**（AI 工具端口常撞：7860/7861/8188/6006 等）：
   - `runner` 新增端口探测（`_port_in_use` bind 探测 / `_next_free_port`）。
   - 启动前若目标端口被占用：webui 引擎（launch.py 等）自动改写 `--port=<空闲端口>`；bat 引擎（webui.bat / 一键启动.bat 等）**非侵入式**注入环境变量 `COMMANDLINE_ARGS="--port=<空闲>"`（A1111 系启动器约定，脚本不支持则忽略并可在日志看到）；多开实例端口偏移后再兜底一次空闲查找。
   - 连续 30 个端口全占 → 明确返回错误「端口被占用，且未找到空闲端口，请到设置更换端口」；演示模式不探测。
2. **启动自检独立小窗（游戏启动器式）**：
   - Electron：主窗口改为按需显示（`?main=1`，App 跳过重复自检门控）；配置 `selfcheck.run_on_startup` 开启时先弹**无边框、屏幕居中、520×700** 的独立自检小窗（`?view=startup`），完成后 IPC 通知主进程关小窗、显主窗；手动关闭小窗（✕/右上角）等价跳过并进入。
   - 外观：顶部拖拽栏（可拖动 + ✕ 跳过）、居中品牌区（渐变 Logo + 发光阴影 + 大标题）、**发光渐变进度条**（x/y 项正常 · pct%）、卡片化检测项（正常/警告/错误配色 + 状态章）、修复询问/版本更新/不可修复提示内嵌滚动区、底部渐变「进入 FTN Atelier」按钮 + 品牌落款。
   - 手动自检（设置→立即自检）保持**盖在主窗口上的弹窗**不变，仅新增进度条。
3. **前端分流**：`main.jsx` 按 `?view=startup` 渲染独立自检页（`pages/StartupCheck.jsx`），否则渲染主应用；`App.jsx` 主窗口加载时跳过启动自检门控（避免重复自检）。
### 验证记录（本轮）
- 前端 `npm run build` ✅（407 modules，含 StartupCheck + SelfCheckModal 独立布局）；Electron `node --check` ✅；后端 `compileall` ✅。
- 新增 `tests/port_conflict_test.py` 5 项全过：占用探测、webui 自动改 `--port`、bat 注入 `COMMANDLINE_ARGS`、演示模式跳过、全段占用明确失败。
- 既有测试（runner / runner_multi / multi_open / console_sessions 等）全过，端口逻辑不影响演示模式与默认互斥。

### 本轮新增大项（引擎 cmd 自动挂载 / 子进程窗口全部隐藏）
1. **移除「手动新建会话」**（产品定论，对齐绘世）：控制台不再提供创建 CMD/PowerShell 会话的入口与后端接口（`POST /api/console/sessions`、input 接口、交互式保活、stdin 管道全部删除）；`console_sessions.py` 收敛为「引擎实例 → 控制台会话视图」的纯聚合层。
2. **引擎启动带出的 cmd 自动挂载**：每启动一个引擎实例（含多开 #2/#3），控制台自动新增标签、名字与引擎对应（reForge / reForge #2 / WD1.4 / LoRA 训练器），实际运行才出现、停止即消失、关闭仅终止对应实例；ftn_tag（html 工具）无 cmd 不进控制台。
3. **子进程窗口全部隐藏**：runner 引擎进程（webui.bat / launch.py / 演示进程）与后台工具命令（git、nvidia-smi、wmic、taskkill 等）统一加 `CREATE_NO_WINDOW`，不再弹独立 cmd 黑窗——与文档「非独立 cmd 窗口」承诺真正对齐。
4. **退出清理简化**：`/api/shutdown` 只停全部引擎实例（无独立会话需清理）；关闭应用拦截逻辑不变。
### 验证记录（本轮）
- 前端 `npm run build` ✅（407 modules）；Electron `node --check` ✅；后端 `compileall` ✅。
- `tests/console_sessions_test.py` 改写为自动挂载模型 4 项全过：启动自动挂载（标题 reForge）、WS 实时推引擎日志、多开自动新增 #2 且关闭 #2 不影响 #1、停止自动消失。
- 既有测试（port_conflict / multi_open / runner / runner_multi）全过。

### 本轮新增大项（主引擎化 / 自由多开 / 自检流程修复 / 外观与合规）
1. **主引擎化**：引擎默认项收敛为单一「主引擎」（key=reforge，label=主引擎，可改名不可删）；WD1.4 / LoRA 训练器 / FTN-tag 库不再预置，需要时「新增引擎」添加；自检文案同步改「主引擎路径 / 主引擎 Python 环境」；未配置时首页占位「暂无主引擎」。
2. **自由多开（取消开关与互斥）**：runner 不再使用全局锁与多开开关；主引擎运行中可继续启动任意引擎或同引擎多开；每实例独立进程/日志/端口/关闭；首页「多开一个」按钮；**实例占用接口** `GET /api/engine/stats`：按进程树汇总内存（ctypes GetProcessMemoryInfo）与显存（nvidia-smi compute-apps），首页灯带下方显示「内存 X MB · 显存 Y GB」（bat 可检测，html 无进程不显示）。
3. **启动自检流程修复**：
   - 后端启动即自动补齐 Core/Engines、Data、Backup、Logs、Database（新环境不再报「目录不完整」）；
   - 修复 12s 兜底误放主窗口：仅当自检小窗**从未出现**时才放主窗口，小窗正常显示时绝不提前进入；
   - 自检按钮文案：进行中「检测中…」→ 完成后有异常「有异常待处理」→ 无异常「进入 FTN Atelier」；**无异常自动进入**（约 1.2s），有异常手动点击进入；
   - 自检小窗 Logo 由 🛠️ 改程序图标（`app:logo` IPC → ico dataURL）。
4. **版本 / 插件**：基底未安装不再标「主」（仅已装主基底标主）；插件市场按当前主基底提供通用/专属插件。
5. **外观动画增强**：粒子数量/光晕/透明度提升、光效光斑增强、呼吸动画明暗范围 0.12↔0.55 + 缩放脉动（更明显）。
6. **关于 / 页脚**：页脚改「生图引擎工作台」；关于页新增「使用声明」（合法用途、禁盗版收费、禁黄赌毒、责任自担）。
7. **CUDA 检测加固**：nvidia-smi 增加 PATH 外常见路径兜底（打包环境 PATH 不完整时也能检测到）。
### 验证记录（本轮）
- 前端 `npm run build` ✅（407 modules）；Electron `node --check` ✅；后端 `compileall` ✅。
- 自由多开测试（重写 runner_multi / multi_open）：主引擎+wd+主引擎#2 三实例并存、关 wd 不影响主引擎、stop_all 全停 ✅。
- 控制台自动挂载回归（主引擎 / 主引擎 #2 标题）✅；`/api/engine/stats` 返回 rss_mb/gpu_mb ✅；自检 11 项文案与目录自动补齐验证 ✅。
- 顺手修复：Dashboard `currentKey` 悬空引用（潜在白屏）。

### 本轮新增大项（v1.0.0 功能增强包）
1. **启动失败诊断**（`core/diagnose.py` + `/api/engine/diagnose`）：引擎启动失败/提前退出时，从日志匹配「端口占用/缺依赖/OOM/显卡驱动/Python 缺失/venv 损坏」并给出可直接照做的修复建议，首页失败时展示。
2. **模型快速扫描**（增量）：`full=false` 时按已索引 size/mtime 跳过未变化文件（几十 GB 模型库不必反复读 safetensors 头）；模型页「快速扫描 / 全量扫描」两个按钮。
3. **首页占用趋势**：`/api/engine/stats` 附带采样历史（最近 5 分钟），引擎行内显示内存微型折线图。
4. **版本切换预览**（`/api/versions/preview-switch`）：切换前确认 venv 策略（复用/重建）与配置文件迁移差异，避免盲切。
5. **插件一键更新全部**（`/api/plugins/update-all` + 任务进度轮询）+ **重复插件检测**（相同 git remote 标记「重复」）。
6. **数据导出/导入**（`/api/data/export|import`，设置→数据管理）：配置 + 引擎注册 + 模型索引一键迁移；新增 `dialog:readTextFile` IPC。
7. **首启简单引导**：未配置主引擎时首页显示可关闭的引导卡（「第一步·配置主引擎」）。
8. **文案自适应**：版本页下载提示按是否有主引擎切换（无主引擎→「下载后自动设置为主引擎」；有→「归为非主引擎」）；插件市场主引擎为空显示「reForge / Forge（自动检测）」。
9. **搜索框可读性**：网络下载搜索框文字/占位符加色；全局 `::placeholder` 颜色保证暗色主题可读。
### 验证记录（本轮）
- 前端 `npm run build` ✅（407 modules）；后端 `compileall` ✅；Electron `node --check` ✅。
- 接口冒烟：diagnose / market(no_engine) / update-all+任务轮询 / data export+import / preview-switch（venv=rebuild、configs 差异）/ models 快速扫描 全部返回正确 ✅。
- 已用最新代码重新打包 `FTN-Atelier-Portable-1.0.0.zip`。

### 本轮新增大项（版本身份最终方案 + 修复检测不到 reForge）
1. **版本身份模型**（`core/version_manager.py` `_read_version`）：`install_source` 三分类（`atelier_managed` / `git` / `external`）；`repository + branch + commit` 为版本身份，正式 tag 仅作友好名称；`dir_name` 里的 `-main` / `-master` 只作 branch 推断，**绝不当 version**；external 无 `.git` 无记录 → version 为空（UI 显示 Unknown），不猜 commit、不做 git init 伪造。
2. **ZIP 接管 / Atelier Managed 更新**：
   - `POST /api/versions/takeover`：外部 ZIP/目录 → 取目标 branch 最新 commit → 下载 ZIP → 临时解压 → 校验（launch.py / webui.py）→ 备份用户数据 → 替换程序文件 → 还原用户数据 → 写 `.ftn/engine.json`；失败自动回滚旧程序目录。
   - `POST /api/versions/managed-update`：atelier_managed 实例复用同一安装例程更新，成功才删旧备份。
   - 前端版本页按钮：external →「接管为 Atelier 管理」；managed →「检查更新」；git →「选择新版 / 回退」。
   - 更新保护：`PROTECTED_PATHS`（models/outputs/extensions/embeddings/config.json/ui-config.json/webui-user.bat/styles.csv/random_res_config）+ 根目录 *.json，`_backup_protected` 移出备份、`_restore_protected` 原样放回（含失败路径，均单测覆盖）。
3. **真 Bug 修复（检测不到 reForge / 版本功能损坏）**：`_read_version()` 重构后不再返回 `source_version` 键，但 `_scan_path_for_base` 与 `_external_primary` 仍引用 `info["source_version"]` → KeyError 被 `except Exception: return None` 吞掉：`_external_primary` 返回 None（外部主引擎实例消失→「检测不到我的 reforge」），`_scan_path_for_base` 抛异常（Core/Engines 下有实例→「版本功能损坏」）。修复：删除两处残留 `source_version` 引用；同时把全模块「吞异常」的排查方式记录为：排查时临时加日志或直接单步调用定位。
4. **启动自检 11 项维持**：后端 `ITEM_LABELS` / `_item_runners` 共 11 项（后端/SQLite/目录/主引擎路径/主引擎 Python/Git/CUDA·显卡/网络/配置/端口/孤儿进程）；前端按 `done || 项数到齐` 判定完成 + 30s 兜底，后端 worker `finally` 置 done —— 不再出现「100% 仍在检测中」。
### 验证记录（本轮）
- 后端 `compileall` 全量 ✅；前端 `vite build` ✅（407 modules）；`electron-builder --dir` 打包 ✅。
- 回归测试：`version_test.py`（演示 3 版本切换）✅、`model_detect_test.py`（16 项）✅、`port_conflict_test.py`（5 项）✅、`engine_registry_test.py` / `data_models_test.py` / `multi_open_test.py` ✅（多开/端口/控制台相关集成测试需演示环境或后端在线，未在真实引擎上误启）。
- 真机验证（FTN_PACKAGED=1 + 配置指向本地 reForge 目录，如 `D:\SD\stable-diffusion-webui-reForge-main`）：`/api/versions` 返回 `reforge installed=True`、count=1、`install_source=external`、`branch=main`、版本空（Unknown），demo=False ✅。
- 自检端到端：11 项全部完成（done=True），CUDA 项识别 RTX 3080 Ti；本机无外网时 network 项 WARN 合理 ✅。
- ⚠️ 注意：`tests/console_sessions_test.py` 等集成测试会真实启动引擎（当前开发配置指向真实 reForge），**不要在用户引擎运行/未授权时执行**；`engine_registry_test` / `data_models_test` / `multi_open_test` 会 `config_manager.reset()` 重置开发配置，跑完需还原 `Database/app_config.json`（主引擎路径/主题/自检开关等）。
- 已重新打包：根目录 `FTN-Atelier-Portable-1.0.0.zip` 与便携文件夹均为最新代码（含上述修复）。

### 本轮新增大项（自定义引擎路径 / 设为主引擎修复）
1. **真 Bug（选择路径无效）**：`engine_registry._set_path` 对自定义引擎写 `dict(conf.engine_paths.extra)[key] = root`——只改了临时副本，路径从未写回配置，前端刷新后回到空。修复为直接写 `conf.engine_paths.extra[key]`，并新增全链路验证（新增引擎→设路径→列表显示 root+自动检测入口）。
2. **主引擎可指向任意引擎**：`engine_custom.primary_key` 记录当前主引擎 key（默认 reforge，缺失时回退默认）；`engine_registry.primary_engine()` 统一解析主引擎条目；`config_manager.quick_folders` / `asset_manager._resolve_roots` / `version_manager._external_primary` / `base_registry.primary_health` 全部改为跟随 `primary_engine()`，不再依赖「key 恰好等于主基底」。`POST /api/engines/{key}/primary`：校验该引擎有有效根目录 → `base_registry.infer(root)` 推断基底（无法断定则拒绝并提示）→ 写 `primary_key` + 同步 `primary_base`。删除保护改为「当前主引擎不可删」。
3. **runner 支持启动自定义引擎**：`_resolve_launch` 从引擎注册表解析任意 key 的 root/kind/entry（自动检测入口），不再硬编码 reforge/wd/lora；webui 类启动参数（端口/显存/显卡/自定义参数）按 kind 判断，Forge 等自定义 webui 引擎同样生效。
4. **前端**：设置→引擎路径「设为主引擎」不再限定 key 为 reforge/forge（有根目录即可显示按钮，后端按基底校验）；成功后横幅显示 `主引擎已切换 → 名称（基底）`。
### 验证记录（本轮）
- 后端 `compileall` 全量 ✅；前端 `vite build` ✅（407 modules）；`electron-builder --dir` 打包 ✅；便携 zip + 根目录文件夹已刷新。
- 自定义引擎全链路沙箱实测：add_engine → set_path（persist 到 config.extra）→ list_engines（root + webui.bat 自动检测）→ set_primary（primary_key=testclone、基底推断 reforge）→ quick_folders 根目录跟随克隆 → version 外部实例=克隆 → runner 解析出 `cmd /c webui.bat`（非演示模式）→ 清理还原 ✅。
- 回归：`engine_registry_test`（增/改名/主引擎保护/删自定义）✅、`version_test` ✅、`multi_open_test` ✅、`model_detect_test` ✅、`port_conflict_test` ✅。

### 本轮新增大项（新增引擎去代号）
1. **前端**：设置→引擎路径「新增引擎」表单删除「代号（key）」输入框，只保留「名称 + 类型（WebUI/启动脚本/Tag 库）」；Enter 直接确认；名称必填校验文案改为「请填写引擎名称」。
2. **后端**：`engine_registry.add_engine` 的 key 参数改为可选——为空时由 `_auto_key(label)` 生成：拉丁字符转小写短横线（如 `Forge 测试`→`forge`）、纯中文/特殊字符回退 `engine-<毫秒时间戳>`、与已有引擎重名自动追加 `-2/-3`；key 全程对用户不可见（改名只改显示名，内部 key 不变，路径/会话/启动引用稳定）。
3. 兼容：显式传 key（旧调用/测试）仍按原逻辑生效并做重名校验。
### 验证记录（本轮）
- 后端 `compileall` 全量 ✅；前端 `vite build` ✅（407 modules）；`electron-builder --dir` 打包 ✅；便携 zip + 根目录文件夹已刷新。
- 新增引擎沙箱实测：中文名→`engine-<ts>`、英文名→`forge`、重名→`forge-2`、新增后 root/入口自动检测正常 ✅；清理后引擎列表干净（仅主引擎）✅。
- 回归：`engine_registry_test` / `version_test` / `multi_open_test` ✅。

### 本轮新增大项（主引擎置顶标识 / 接管修复 / 全局检修）
1. **主引擎永远置顶 + 「主」标识**：`engine_registry.list_engines()` 末尾稳定排序 `primary 在前`（其余保持自定义顺序）；首页（Dashboard）引擎行加「主」徽标（主色高亮小标签），且引擎列表加载后默认选中主引擎（不再写死 reforge）；设置页引擎列表天然跟随置顶，主引擎行原有「主引擎 · 不可删」徽标不变。
2. **接管「未知基底仓库」修复**：`_base_of_path` 对 Core/Engines 之外的路径原先返回 `..`（假基底），`_repo_of("..")` 取空 → 报「未知基底仓库」；现外部路径返回 None，`takeover_external` 的基底解析改为 `路径首段 → base_registry.infer(根目录) → 兜底 reforge`；`_gh_commit_sha`（GitHub API）与 ZIP 下载统一走 `_mirror_url` 镜像。
3. **安装例程安全加固（`_install_job`，高风险核心）**：
   - 临时目录改到与引擎**同盘**（`mkdtemp(dir=引擎父目录)`）：models/outputs 几十 GB 跨盘 move 会变成复制+删除（慢、占双倍磁盘）；
   - 替换程序失败：**先恢复旧程序目录，再统一还原用户数据**（原实现失败路径会丢用户数据）；
   - 用户数据还原失败：把备份**挪出临时目录**（`.ftn-userdata-backup-<ts>`，同盘父目录）再抛错，绝不随 `finally` 的 rmtree(tmp) 一起删除；
   - 解压包根目录探测健壮化：优先选「含 launch.py/webui.py」的子目录（兼容镜像二次打包的平铺 zip）。
4. **全局检修（本轮）**：
   - 吞异常点补日志：`_external_primary` / `_scan_path_for_base` 异常不再静默（写 `Logs/` warning），单实例损坏只跳过该实例，不再拖垮整个版本列表；
   - 自检「端口」项从“恒返回 ok”改为真实 `socket.create_connection` 探测；
   - 移除前端未使用的 `basesSetPrimary`；前端 64 个 `/api/*` 引用与后端 78 条路由交叉核对零缺失；
   - Electron `main/preload/backendManager` `node --check` ✅；
   - 新增回归测试 `tests/engine_primary_install_test.py`（7 项：路径持久化 / 自动 key / 主引擎置顶 / takeover 预检 / 安装成功 / 校验失败不动目录 / 还原失败救备份），全过。
### 验证记录（本轮）
- `engine_primary_install_test.py` 7/7 ✅；`engine_registry_test` / `version_test` / `port_conflict_test` / `multi_open_test` / `runner_test` / `runner_multi_test` / `watchdog_test` / `asset_scan_test` / `check_tags` 全过；`model_detect_test` 16/16 ✅。
- 后端 `compileall` ✅；前端 `vite build` ✅；`electron-builder --dir` ✅；便携 zip + 根目录文件夹已刷新。
- ⚠️ 集成测试（console_sessions / ws_test）需在线后端且会真实启动引擎，未在真实 reForge 上执行；测试类脚本会重置开发配置，跑完需还原 `Database/app_config.json` 与 `engine_custom` / `active_engine`。

### 本轮新增大项（reForge / Forge 仓库分离）
1. **配置源**：`base_registry._BASE_DEFS`（主）与 `version_manager._BASE_DOWNLOAD`（兜底）分别填写两个官方仓库：
   - reForge → `https://github.com/Panchovix/stable-diffusion-webui-reForge`
   - Forge → `https://github.com/lllyasviel/stable-diffusion-webui-forge`
2. **消费点核对（全部经 `_repo_of(base_key)`）**：版本列表行 `repository`（`_read_version` / `_scan_path_for_base` / `_external_primary`）、`snapshot.download_url`（本轮改为 `_repo_of`，不再直接读 `_BASE_DOWNLOAD`）、`download_info` / `download_candidates`（GitHub 实时 tags）、`start_download`（浅克隆）、git 更新/回退（`_resolve_remote_tag`）、`takeover_external` / `managed_update`（ZIP 接管/更新，GitHub API commit + archive 均按基底仓库）。插件市场为独立策划库（`plugin_library.py`），与应用仓库无关；软件自检更新走 `selfcheck.update_owner/repo` 配置，与引擎无关。
3. **接管语义变化**：外部 reForge ZIP 点「接管为 Atelier 管理」后将安装 reForge 官方仓库 `main` 分支（不再误装 Forge）。
### 验证记录（本轮）
- `_repo_of("reforge")` / `_repo_of("forge")`、两处配置源、外部 reForge 目录 `_read_version.repository`、`snapshot` 各基底 `download_url` 全部指向各自官方仓库 ✅；`engine_primary_install_test.py` 7/7 ✅；后端 `compileall` ✅；前端 `vite build` ✅；便携 zip + 根目录文件夹已刷新。

### 本轮新增大项（版本板块检修 / 接管网络兜底）
1. **接管「Expecting value」修复**：`_gh_commit_sha` / `_download_zip` 原来只走 `_mirror_url`（ghproxy 对 `api.github.com` 返回 HTML → `json.load` 报 `Expecting value: line 1 column 1`）。现统一改为 `_url_candidates()`：**直连优先 → 镜像兜底 → 都不通才抛明确错误**（错误含原因，不再甩原始 JSON 解析异常）。
2. **版本板块检修**：
   - `_fetch_tags`（版本候选拉取）同样补镜像兜底：直连失败自动尝试 ghproxy，都失败才退静态兜底候选；
   - `download_candidates` 跳过空版本（未知）已装实例，候选/已装列表不再出现空白版本行；
   - `update_version` / `rollback_version`：对 Core/Engines 之外的 git 实例用 `base_registry.infer(root)` 兜底基底，仓库地址不再取空（原 `_repo_of(None)`→`""`，目标 tag 解析不准）；
   - 前端 `fmtVer` 补 `v.branch`：Atelier 管理版本显示 `main @ abc1234`（原只显示 commit）；
   - 前端更新候选过滤加空值防御。
3. **回归固化**：新增 `tests/version_audit_test.py`（仓库分离 / `_fetch_tags` 直连→镜像 / 候选跳空版本 / 外部实例 update+rollback 基底兜底）5 项全过；`engine_primary_install_test.py` 扩至 8 项（新增 commit/ZIP 直连→镜像兜底）。
### 验证记录（本轮）
- `version_audit_test.py` 5/5 ✅；`engine_primary_install_test.py` 8/8 ✅；`version_test` / `port_conflict_test` / `multi_open_test` / `model_detect_test`（16 项）/ `engine_registry_test` ✅；后端 `compileall` ✅；前端 `vite build` ✅；便携 zip + 根目录文件夹已刷新。
- ⚠️ 注意：`version_audit_test.py` 会在 Core/Engines 下建临时基底目录做候选测试，已自愈清理（测试后 `Core/Engines` 仅剩 `.gitkeep`，演示模式判断不受污染）。

### 本轮新增大项（接管 commit 获取改用 git ls-remote）
1. **问题**：用户环境 `api.github.com` 直连与 ghproxy 镜像都返回 HTML（被墙/镜像失效），REST API 的 `json.load` 报 `Expecting value: line 1 column 1`，接管在「获取远端最新 commit」步必失败。
2. **修复**：`_gh_commit_sha` 弃用 GitHub REST API，改用 `git ls-remote <repo> refs/heads/<branch>` 取分支最新 commit SHA（直连优先→镜像兜底→都不通才明确报错）；git 协议走 HTTPS，输出为纯文本 SHA，彻底消除 JSON 解析失败这一类问题。ZIP 下载保持直连→镜像。
3. **提示**：若用户环境 GitHub 直连与 ghproxy 均不通，可在 设置→环境配置→下载镜像 更换 Git 镜像前缀（ghproxy.com 时常失效，可换 gh-proxy.com / ghfast.top 等）。
### 验证记录（本轮）
- `engine_primary_install_test.py` 9/9 ✅（新增 [9] commit 获取全部失败→明确报错）；`version_audit_test.py` 5/5 ✅；后端 `compileall` ✅；前端 `vite build` ✅；便携 zip + 根目录文件夹已刷新（包内已确认含 git ls-remote 实现）。

### 本轮新增大项（仓库地址双重拼接修复 + 多镜像自动轮换）
1. **真 Bug（`github.com/github.com` 双重拼接）**：`takeover_external` 从 `_repo_of()` 取到的是**完整 URL**（`https://github.com/Panchovix/...`），但 `_gh_commit_sha` / `_zip_url` 又拼了一次 `https://github.com/{repo}` → `https://github.com/https://github.com/...`，镜像再套前缀变成三层，`git ls-remote` 退出码 128。修复：新增 `_repo_path()` 把仓库地址统一规整为 `owner/name`，两个函数（及接管/在线更新共用路径）全部走规整后拼接；测试断言 `github.com/github.com` 永不出现。
2. **多镜像自动轮换（对齐绘世）**：`_url_candidates` 返回 **直连 → 配置镜像（设置→环境配置可改）→ 内置常用镜像（gh-proxy.com / ghfast.top / mirror.ghproxy.com）** 的去重候选列表，commit 获取与 ZIP 下载逐个尝试，都不通才明确报错；`_mirror_url` 兼容前缀已带 `https://github.com/` 的历史/手填配置（自动去掉，防双重拼接）。
### 验证记录（本轮）
- `_repo_path` 短/长两种写法均规整为 `owner/name`；候选列表 5 个（直连+4 镜像）无重复、无 `github.com/github.com` ✅；`engine_primary_install_test.py` 9/9 ✅（[8] 覆盖完整 URL 传参 + 配置镜像失效后轮换到内置镜像）；`version_audit_test.py` 5/5 ✅；后端 `compileall` ✅；便携 zip + 根目录文件夹已刷新。

### 本轮新增大项（接管引擎选择式更新 / 备份保留 / 去掉 0B）
1. **Atelier Managed 选择式更新/回退**：`managed_candidates(engine_id)` 检测当前记录（branch/commit/tag）→ 远端分支最新 commit（git ls-remote）→ 仓库 tags（`_fetch_tags`，直连→镜像），返回 `update`（最新分支 + 高于当前 tag 的 tag）与 `rollback`（低于当前 tag 的 tag；当前无 tag 时列出全部 tag）；前端版本行对 managed 实例显示「↑ 选择新版 / ↶ 选择回退」（与 clone 版一致），候选展示在选择弹窗，用户选定后 `managed_update(id, target)` 按分支最新或指定 tag 安装（`_install_job` 支持 `refs/heads/*` / `refs/tags/*`，记录 tag+commit）；已是最新时弹窗明确提示“无需重复更新”，不再像旧「检查更新」那样无条件再下载一次。
2. **备份处理（接管/更新成功后）**：用户数据（models/outputs/extensions/embeddings/localizations/venv/配置）还原到引擎目录，无残留；**旧程序备份**不再随临时目录删除，改为移动到 `Backup/engine-backups/<引擎名>-<时间戳>/`，自动只保留最近 2 份、更旧的清理，完成提示会写明备份位置；失败路径回滚与用户数据救援逻辑不变。
3. **版本行 0B**：外部/接管实例 size 为 0（避免扫描几十 GB 目录），前端 `v.size > 0` 才显示体积，不再出现无意义的 `0B`。
### 验证记录（本轮）
- `version_audit_test.py` 7/7 ✅（新增 [6] managed_candidates 候选检测、[7] managed_update 分支/tag 选择）；`engine_primary_install_test.py` 9/9 ✅（适配 ref 签名）；后端 `compileall` ✅；前端 `vite build` ✅（407 modules）；便携 zip + 根目录文件夹已刷新（包内已确认含 managed_candidates 与 engine-backups）。

---

## 七、未完成 / 后续方向（按优先级建议）

### 高优先级（与日常使用最贴合）
- ✅ **输出目录「不分日期直接扔根目录」**：监听 + 迁移方案（`core/output_watcher.py` 后台整理，设置·输出整理 Tab 开关/立即整理）。
- ✅ **模型卡片完善**：底模/LoRA 竖版 2:3 预览图、embedding/vae 独立卡片样式（图标带、无大图占位）、大小、基础模型 badge、触发词、文件更新日期、📂打开文件夹。
- ✅ **VAE / Embedding 预览图**：已定论**均不做**——VAE 由文件名即可识别（`vae-ft-mse-*` / `kl-f8-*` 等），Embedding 是纯向量权重（`easynegative`/`badhandv4` 等，文件既小而规则、且本就不该有图）。两者沿用「简单卡片样式（图标带、无大图）」。此设计非遗留任务。

### 中优先级
- ✅ **控制台自动挂载引擎 cmd + 自由多开**（本轮完成）：内置控制台不做手动新建会话，引擎启动带出的 cmd **自动挂载**（每开一个自动新增标签、名字与引擎对应、实际运行才出现、关闭仅终止对应实例），引擎进程窗口已隐藏；**自由多开**（无开关无互斥）：任意引擎可叠加启动，端口自动避开，首页显示内存/显存占用。
- ⬜ **CivitAI / HuggingFace 网络下载**（搜索→下载→加入索引）。✅ 已完成（本轮）：独立菜单页 + `core/downloads.py`（CivitAI 搜索/类型过滤、HF 搜索/文件选择、流式后台下载+进度、自动落入分类目录并重新索引）；路由 `/api/downloads/sources|search|files|start|status`。
- ⬜ **reForge 更新流程**：无缝更新、保留设置，版本备份/回滚。✅ 已完成（本轮）：真实 git 下载（浅克隆）/ 更新（fetch+checkout 最新候选）/ 回退（unshallow+checkout 旧 tag），后台任务 + 进度轮询；前端版本页 active 行操作。
- 🟡 **插件管理完善**：查看插件版本详情、开关后重启进程按钮上下文等（浏览/开关/开目录/市场下载更新卸载已实现）。

### 低优先级 / 锦上添花
- ✅ **外观动画**：粒子/光效/呼吸（随主色调同色，非活动窗口自动暂停）已完成——`preference.ambient_effect` + `pages/AmbientLayer.jsx`（Canvas 粒子/光效 + CSS 呼吸），主题页「外观动画」分区 + 设置·用户偏好下拉可选，`AppContext` 监听窗口活动。
- ✅ **环境检测链完整化**：Python/Git/CUDA/显卡/模型路径一键检测——`core/env_detect.py`（`/api/system/env`）只读快照展示于疑难解答页「环境检测」分区；启动自检链并入 **CUDA·显卡** 项（现 11 项）。显存已并入 GPU 检测（nvidia-smi）。
- ⬜ **导出目录整理/迁移模块**（若原生无法取消日期子目录）。

---

## 八、技术栈 & 目录职责备注

- **前端**：Electron(主进程 `electron/main.cjs` + preload `preload.cjs`)｜React+Vite+Tailwind｜页面 `src/pages/*`｜状态 `state/AppContext.jsx`｜API `services/apiClient.js`｜WS `hooks/useSocket.js`｜主题 `themes.js`。
- **后端**（`Source/backend/`）：FastAPI 常驻｜核心模块 `core/`（runner 引擎控制、console_sessions 控制台会话、asset_manager 模型索引、version_manager 版本/插件、engine_registry 引擎条目、config_manager 配置、log_manager 日志、db SQLite、status、host_watchdog）。
- **Git 规则**：Core/Engines、Data、Backup、用户资产目录必须 .gitignore，禁止提交模型/输出/引擎实例/用户配置备份。
- **目录**：根下 Logs/（运行日志）、Core/Engines（被管引擎）。

---

*重要提示：本工程目前处于可运行状态（构建通过、后端常驻、无白屏），作为个人使用的生图引擎工作台核心功能（启动/自由多开/主题/设置/版本/控制台/疑难解答/小工具/插件管理/插件市场）已完整可交互。未完成项集中在真实引擎环境联调、插件管理完善与正式发布配置。*

---

## 九、如何启动运行测试（重要 · 记住这个）

> 这是之前「顺带打开 exe」的方法，务必记住。**坑在环境变量 `ELECTRON_RUN_AS_NODE`**：若其残留为 `1`，`electron.exe` 会以纯 Node 模式运行 → **不开窗口、报 `app is not defined`**。因此凡是启动前都要先清掉它。

### 方式一：双击脚本（推荐，最省事）
工程根目录已有 **`启动开发.bat`**，脚本内部已自动清除 `ELECTRON_RUN_AS_NODE` / `ELECTRON_ENABLE_LOGGING`，并依次：
1. 安装依赖（如需 `npm install`）
2. `npm run build`（构建前端）
3. `start electron.exe <frontend 目录>` 打开应用

### 上传 GitHub（一键脚本）
工程根目录 **`上传GitHub.bat`**：双击后自动检查变更 → 确认 → 暂存 → 提交 → 推送 `origin main`。
安全约定：默认只暂存「已跟踪文件的修改/删除」（`git add -u`）；未跟踪新文件单独列出、需用户明确确认才加入；
暂存列表含 `.zip/.exe/.db` 等大文件/打包产物时自动停止；不使用 `--force` / `reset --hard` / `clean`；
依赖 `.gitignore`（Database/Logs/Portable/zip 均已忽略）。任一步失败立即停止。
可用参数指定仓库目录便于测试：`上传GitHub.bat <仓库目录>`。

### 方式二：PowerShell 手动（调试/自动化时用）
在 `Source/frontend` 目录下执行（先构建，再清变量启动，无需 `start` 也能保持在本会话）：

```powershell
cd Source/frontend
npm run build                 # 先构建最新前端（可省略，直接跑已构建产物）
$env:ELECTRON_RUN_AS_NODE=$null   # 必须清空，否则 electron 跑成 Node、不开窗口
$env:ELECTRON_ENABLE_LOGGING=$null
Start-Process -FilePath "node_modules/electron/dist/electron.exe" -ArgumentList "."
```

- electron 可执行文件路径：`Source/frontend/node_modules/electron/dist/electron.exe`
- 应用会自动拉起/连接 FastAPI 后端（端口默认 `19000`），后端常驻，健康检查：`http://127.0.0.1:19000/api/health`
- 查看运行日志：`Logs/`（根目录下）

### 停止运行
- **优雅**：`Invoke-RestMethod -Uri http://127.0.0.1:19000/api/shutdown`（通知后端退出）
- **强制**（异常残留时）：`Get-Process electron | Stop-Process -Force`

### 验证是否启动成功（无白屏）
```powershell
Invoke-RestMethod -Uri http://127.0.0.1:19000/api/health   # 后端应返回 status=ok
```

---

## 十、如何打包发布（重要 · 记住这个）

### 方式一：双击脚本（推荐）
工程根目录已有 **`打包.bat`**，脚本内部自动：
1. 清掉 `ELECTRON_RUN_AS_NODE` / `ELECTRON_ENABLE_LOGGING`（否则 electron 以纯 Node 运行、不开窗口）；
2. 配置 electron-builder 下载镜像（npmmirror，GitHub 直连不通时走镜像，首次下载后缓存）；
3. `npm install`（如需）→ `npm run build:prod`（vite build + electron-builder `--dir`，仅产出免安装目录）。

产物：
- **工作区根目录 `FTN-Atelier-Portable-1.0.0.zip`** —— **开箱即用版（对齐绘世，唯一产物）**：解压后运行 `FTN Atelier.exe` 即可，自带嵌入式 Python 运行时（`resources/backend/runtime`，含全部后端依赖），**目标机无需安装 Python、无需任何依赖**；正式打包关闭演示数据（版本/引擎不造假）
- `Source/frontend/release/win-unpacked/FTN Atelier.exe` —— 免安装版中间目录（构建生成，打包脚本压缩为 zip 后自动清理）

### 打包配置要点（package.json `build` 字段）
- `files`: 只打包 `dist/**` 与 `electron/**`（不含 node_modules，前端已构建成静态产物）；
- `extraResources`: 把 `Source/backend` 整体打进 `resources/backend`（排除 `__pycache__`/`venv`）——**后端随包分发**；其中 `runtime/` 为内置嵌入式 Python 3.10.11 + 后端依赖（pip 装自清华镜像），实现开箱即用；
- `electronDist`: 指向本地 `node_modules/electron/dist`，跳过 Electron 下载；
- `win.target = "dir"`：**只产免安装目录**（不再生成 NSIS 安装包）。

### 便携文件夹同步规则（重要 · 防止误删运行时数据）
- 根目录 `FTN-Atelier-Portable-1.0.0/` 是**可直接运行**的便携版副本；`FTN-Atelier-Portable-1.0.0.zip` 是分发产物（zip 只含程序文件，不含运行时数据）。
- 升级便携文件夹时：**只覆盖程序文件（win-unpacked 内容），绝不删除/移动 `Core/`、`Data/`、`Database/`、`Logs/`**——这些是运行时产生的引擎实例/索引/配置/日志，可能含用户数据，必须原样保留。
- 校验方式：同步后检查便携文件夹仍保留 `Core/Engines` 下已有实例。

### 打包版运行行为（与开发版差异）
- **数据目录重定向**：打包后 Electron 注入 `FTN_APP_DIR`，后端把 Database/Logs 写到
  `%APPDATA%\ftn-studio-frontend`（与开发模式源码目录的数据相互独立，互不污染）。
  后端路径统一经 `core/paths.py` 的 `app_root()` 解析。
- **Python 运行时**：解释器解析顺序 = 内置 `resources/backend/runtime/python.exe`（开箱即用）→ `venv` → 系统 `python`；
  便携版已自带运行时，目标机无需安装 Python；若要重建运行时：
  直接运行工程根目录 **`重建运行时.bat`**（下载 python-3.10.11-embed → 启用 `import site` → get-pip →
  `pip install -r requirements.txt`（清华镜像）），完成后重新打包。`runtime/` 为生成物，已在 .gitignore 忽略。
- **启动自检小窗**：全新环境无配置文件，`selfcheck.run_on_startup` 默认开 → 启动时弹独立自检小窗。
- **端口冲突自动处理 / 多开 / 关闭拦截**：与开发版一致。

### 排障提醒
- 打包版 exe 双击后**瞬间退出且无窗口**：99% 是 `ELECTRON_RUN_AS_NODE` 残留，先 `set ELECTRON_RUN_AS_NODE=` 再运行。
- 主进程启动链路已内置文件探针：设 `FTN_BOOT_LOG=C:\xxx\boot.log` 运行即可看到逐步日志（main.js → ready → 后端拉起 → 窗口创建）。
- 数据看不到/找不到：确认用的是 `%APPDATA%\ftn-studio-frontend`（打包版）而不是源码根目录（开发版）。

---

*最后提醒：下次要继续开发，直接按「九、如何启动运行」打开，或看看「七、未完成」的优先级清单挑任务。*
